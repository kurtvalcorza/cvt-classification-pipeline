# Release verification

`tutorials/cvt_classification_colab.ipynb` (`E2E`, **standalone** carrier) is a **release candidate** until the exact notebook revision has executed top-to-bottom in a clean supported runtime. Unit tests, the tiny-model test, JSON validation, code-cell compilation, the generator parity checks and `tools/validate_release_assets.py` are necessary checks but are **not** runtime evidence under DIMER Notebook Specification 2.1 (REL8). This file is the durable release-gate record for the notebook.

The upstream snapshot is pinned to `84e365a5f6a5ca987486abb25f3d8e5265cdc44d` and the notebook is regenerated with that revision and manifest, so it can run. One default-path execution is recorded below (2026-09-26, Kaggle T4); release step 7 (REL12) has not been exercised, so the status stays `Candidate`.

## Automatic coverage (static and unit, every pull request)

CI installs the pinned CPU-only torch wheel and the other runtime pins, then runs:

- `ruff check src tests tools`;
- `pytest`: snapshot verification and staging against synthetic manifests, the unpinned refusals, the import boundary, input and dataset validation, BYOD directory reading, average precision, the evaluation report, the notebook parity checks, the weight-facts check, the pin tool against a fake Hub, the data module against synthetic archives and folders (member-path refusals, size bounds, digest verification, stratified splits), the strict loader against a full-size random checkpoint, the torchvision transform against the Transformers processor, and `tests/test_small_model.py` — a small random-weight `CvtForImageClassification` taken through `predict`, `zero_shot_evaluate`, full and frozen `finetune`, `evaluate`, `save_artifact` and `apply_artifact`;
- `tools/validate_release_assets.py`: model card 1.2 structure and front matter, the pin state across the package, the manifest and the documents, identity consistency, the weight facts, the release-status tokens, and the notebook's structure, carried modules, parity, markers, BYOD gates and location fields;
- `tools/build_notebook.py --check`.

These are source, provenance and unit checks. None of them loads the pinned checkpoint, so none of them is execution evidence.

## Supported release verification procedure

Before changing the registry status from `Candidate` to `Release-grade`:

1. confirm the snapshot is pinned (`MODEL_REVISION` is a 40-hex commit and every manifest entry has a SHA-256) and that static CI is green on the exact commit under review;
2. open that exact notebook revision in a new GPU runtime (Colab or Kaggle, Tesla T4 or similar) with **no repository checkout** and a clean model cache;
3. run the notebook top-to-bottom without editing implementation cells, with every form field at its default (`USE_BYOD_IMAGE = False`, `USE_BYOD_DATASET = False`, `PER_CLASS = 200`, `EPOCHS = 5`, `FREEZE_BACKBONE = False`);
4. verify that Section 1 reports `NOTEBOOK_SOURCE.repository_revision` equal to `metadata.dimer.generated_from.revision`, and that the installed versions equal the inline `PINS`;
5. verify that every default-path stage completes: the pinned install; the carried modules; staging of all four manifest entries and `verify_snapshot`; the sample archive download with its size and SHA-256 check; `validate_dataset`; the three-way split with its leakage assertion; `validate_inputs` with the `top_k=0` rejection finding; ImageNet top-5 predictions and the `sample-sanity` report; the zero-shot baseline; the blank and noise probes; the untrained-head baseline; `finetune`; the held-out comparison; the unseen split; adapter export, reload and the tolerance check; and the five outputs in `outputs/`;
6. record the number of images per class read from the archive, the held-out accuracy and balanced accuracy of the majority-class baseline, the zero-shot mapping, the untrained head and the fine-tuned model, the unseen-split scores, the per-epoch losses, the trainable and total parameter counts, the degenerate-probe answers and the wall time in the table below. No metric value is asserted in advance: a fine-tuned score at or below the zero-shot baseline is a finding to record, not a failure by itself;
7. exercise the BYOD dataset branch once with a small directory or `.zip` of class folders (`BYOD_DATASET_PATH`) and once with an incompatible one (for example a class with a single image), and record both outcomes (REL12);
8. record the notebook Git blob id, commit, runtime (platform, GPU, Python, PyTorch, torchvision, Transformers), model identifier and revision, the sample archive's SHA-256, whether the model cache was clean, and any warning judged harmless with the reason;
9. record no access tokens or other secrets.

A known-failing default path in the supported runtime blocks release.

## Recorded executions

Notebook identity is the Git blob id of `tutorials/cvt_classification_colab.ipynb` (verify with `git rev-parse <commit>:tutorials/cvt_classification_colab.ipynb`). Each record uses the fields Date, Subject, Runtime, Procedure, Observed result and Caveats.

| Date (UTC) | Subject (commit / notebook blob) | Runtime | Procedure | Observed result | Caveats |
|---|---|---|---|---|---|
| 2026-09-26 (kernel 2026-09-26 00:06:25 – 00:11:08) | commit `5e0f03b` / blob `5d9e491cd649` (`NOTEBOOK_SOURCE.repository_revision` `9a914ff`, the source revision the notebook was generated from, equal to `metadata.dimer.generated_from.revision`; `9a914ff..5e0f03b` changes only the notebook and `tests/test_pin_snapshot.py`) | Kaggle Tesla T4 (`kurtvalcorza/dimer-nb2-cvt-classification` v1), batch run; image `gcr.io/kaggle-gpu-images/python@sha256:37c64f7dd9c54116ecd1bcc88817c5469b88387388fade02bfa8bf3fc647d461` (image torch 2.10.0+cu128), Python 3.12.13, Tesla T4 15360 MiB, driver 580.159.04; after the inline pins torch 2.14.0+cu130 (CUDA 13.0), torchvision 0.29.0+cu130, Transformers 4.57.6, numpy 2.5.3, Pillow 11.3.0 (equal to `PINS`); device `cuda:0`, float32 | serial suite: blob fetched at the 40-char SHA and Git-blob verified, model cache clean at start, no repository checkout; top-to-bottom with every form field at its default (`USE_BYOD_IMAGE = False`, `USE_BYOD_DATASET = False`, `PER_CLASS = 200`, `EPOCHS = 5`, `FREEZE_BACKBONE = False`, `SEED = 0`); `microsoft/cvt-13` at `84e365a5f6a5ca987486abb25f3d8e5265cdc44d` (apache-2.0), 4 manifest files (80,238,966 B) staged and `verify_snapshot` passed (`verified_files` 4); sample archive `Cleanlab/cifar-10-subset` @ `bb5a7aabf1d1` (mit), 986,707 B, sha256 `66f90a4f87d865e8eb653b62f10e754684075a32314177de76832349d4b1fb19`, fetched and verified | **PASSED (default path)** — 279.5 s wall (pass 1 203.4 s stopped after the install cell with `RuntimeError: Core dependencies changed while older modules were loaded` (cuda-bindings 12.9.4 → 13.4.3, numpy 2.0.2 → 2.5.3), kernel restarted after the install cell as the notebook instructs; pass 2 76.0 s), 14/14 post-restart code cells ok. `validate_dataset`: 400 records, 200 images per class (`frog`, `truck`), 32×32 px, accepted with the finding of **100 groups of pixel-identical images**. Split (seed 0): train 280 / held-out 60 / unseen 60, train majority `frog`, ID leakage assertion passed. `validate_inputs` accepted the 4-image sample with the `top_k=0` rejection finding. ImageNet top-5 on the 4 sample images: frogs → `screw` 0.288 and `platypus` 0.286 (no frog class at top-1 or in either top-5), trucks → `moving van` 0.738 and 0.237; `sample-sanity` group hit rate top-1 0.5, top-5 0.5. Held-out (60 images, 30 per class) accuracy / balanced accuracy: majority class 0.500 / 0.500; zero-shot ImageNet mapping 0.9833 / 0.9833 (confusion [[29, 1], [0, 30]], mean group mass 0.3140); untrained two-class head 0.250 / 0.250 (recall frog 0.3333, truck 0.1667); fine-tuned 1.000 / 1.000 (confusion [[30, 0], [0, 30]], mean top-1 score 0.9977). Unseen (60 images): fine-tuned 0.9833 / 0.9833 (confusion [[29, 1], [0, 30]]: `original_images/frog/image_50.png` → `truck` 0.7397), mean top-1 score 0.9920. Degenerate probes, ImageNet head: blank → `nematode` 0.002, `switch` 0.002, `hook` 0.002; noise → `jellyfish` 0.016, `nematode` 0.015, `kite` 0.013; **adapted head: blank → `frog` 0.546, noise → `frog` 0.683**. `finetune`: full model, 19,613,250 of 19,613,250 parameters trainable, 5 epochs, batch 16, AdamW lr 0.0001, weight decay 0.01, no augmentation; epoch losses 0.4279, 0.2589, 0.2276, 0.1724, 0.1632; fine-tune cell about 20 s. Adapter `cvt_adapter.safetensors` 78,629,904 B, sha256 `a68da113fdc1…`, 459 tensors, base state digest `71576c56ac8a…`; reload check 60 images, tolerance 0.0001, equivalent. BYOD refusals shown as expected (non-image object → `TypeError`; class with one image → `ValueError`). Five outputs written, sha256 `cvt_classification_result.json` `117d01543318…`, `…_evaluation_report.json` `77ae45a308f9…`, `…_input_manifest.json` `7e77ca523cc5…`, `…_predictions.csv` `419d877512ab…`, adapter `a68da113fdc1…` | One seeded split of 60 held-out and 60 unseen CIFAR-10 thumbnails (32 px, upsampled to 224 px), one runtime, one pass, no dispersion estimate. The zero-shot ImageNet mapping already scores 0.9833 on held-out, so the fine-tuned 1.000 is a gain of one image (about 1.7 points), within the noise of a 60-image split; the untrained head (0.250) is below the majority baseline, as a random head can be. **Near-duplicate leakage:** the archive holds 100 pixel-identical groups (an `original_images` file and its same-numbered `darkened_images` file) and `split_dataset` splits by ID only, so held-out and unseen images can have exact copies in the training split: by the IDs in `…_predictions.csv`, 46 of 60 held-out and 36 of 60 unseen images have their counterpart (`original_images` ↔ `darkened_images`) in no evaluation split, hence in train (a count of counterparts, not of confirmed pixel copies). The held-out and unseen scores are therefore not evidence of generalisation. The adapted head has no reject option and put 0.546 on `frog` for a blank image and 0.683 for noise. Both frog sample images got non-frog ImageNet top-1 labels (`screw`, `platypus`). The pip resolver's conflict warnings for preinstalled Kaggle packages were harmless (the notebook's version check passed after the restart). **REL12 not exercised** (step 7: BYOD dataset branch with a small class-folder directory and with an incompatible one), so the status stays `Candidate`. No tokens or secrets recorded. |
