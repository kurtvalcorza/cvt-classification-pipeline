# Release status

Current status: **Candidate — source complete, snapshot pinned, no execution recorded**. The pipeline package, the offline test suite (including a small random-weight `CvtForImageClassification` run through prediction, the zero-shot baseline, full and frozen fine-tuning, evaluation and adapter reload, and a full-size random checkpoint taken through the strict loader), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.2) and the standalone `E2E` tutorial `tutorials/cvt_classification_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.1) exist. The upstream snapshot is pinned: `MODEL_REVISION` is `84e365a5f6a5ca987486abb25f3d8e5265cdc44d` and the manifest records every file's byte size and SHA-256 (`model.safetensors` `71576c56ac8a…`, matching the Hub's LFS record). No execution with the pinned weights is recorded in `docs/release-verification.md`, so the repository is not release-grade.
